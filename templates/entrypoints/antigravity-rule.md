# Personal Second Brain Workspace

Natural language is the primary interface. Infer the matching brain operation without requiring a slash command, skill name, or CLI syntax from the user. Use `uv run --project Protocol sb ...` internally for all brain operations. Do not reproduce collection or publishing logic in a workflow, and never write to configured source projects or agent-history roots.
